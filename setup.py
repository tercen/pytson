#!/usr/bin/env python

# from distutils.core import setup
#
# setup(name='pytson',
#       version='1.0',
#       py_modules=[],
#       python_requires='>=3.6',
#       )

from setuptools import setup, find_packages

setup(
    name='pytson', version='1.0',
    packages=find_packages(),
)
